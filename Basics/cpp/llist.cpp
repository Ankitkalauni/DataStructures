#include<iostream>
#include<list>
using namespace std;



int main() {
    // list

    // doublely link-list

    // have to travers through the list to find the element or value
    list<int> lis;

    lis.push_back(3);
    lis.push_front(2);

    return 0;
}
